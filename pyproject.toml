[project]
name = "gonaj-backend"
version = "0.1.0"
requires-python = ">=3.14"
description = "Crowd-sourced GTFS portal backend (Django + PostGIS + uv)"
dependencies = [
    "django==5.2.9",
    "djangorestframework==3.16.1",
    "psycopg2-binary==2.9.11",
    "gunicorn==23.0.0",
]

[tool.uv]
[tool.uv.dependency-groups]


